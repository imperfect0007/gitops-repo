apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: tenant-b
  labels:
    app: nginx
    tenant: tenant-b
spec:
  type: ClusterIP
  selector:
    app: nginx
    tenant: tenant-b
  ports:
    - port: 80
      targetPort: 80

apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: tenant-b
  labels:
    app: nginx
    tenant: tenant-b
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
      tenant: tenant-b
  template:
    metadata:
      labels:
        app: nginx
        tenant: tenant-b
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
